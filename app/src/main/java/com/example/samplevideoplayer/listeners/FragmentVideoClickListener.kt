package com.example.samplevideoplayer.listeners

import androidx.lifecycle.MutableLiveData
import com.example.samplevideoplayer.model.VideoInfo

public interface FragmentVideoClickListener {
    fun onVideoClicked()
}